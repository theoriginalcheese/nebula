# t009
status: done

## Changed

- `nebula-v4.spec` — onefile windowed build of `spike/app.py` → `dist/Nebula-v4.exe`; bundles `spike/web/*`, icon, pywebview/pystray/PIL/psutil/keyboard/websocket hidden imports; excludes `design/` mockup tree
- `spike/app.py` — `INDEX` resolves via `RESOURCE_DIR/spike/web/index.html` (PyInstaller flattens the entry script to `_MEIPASS/app.py`, so `dirname(__file__)/web/` missed the bundle); `consume_goto_pane` skips the dev-only `shots/` file hook when frozen
- `V4-GUIDE.md` — step 11 packaging note (build command, data-dir behaviour)

## Deliberate deviations

- **`collect_submodules("webview.platforms")` warns on android** — harmless; Windows build uses `edgechromium` only.
- **Left `nebula.spec` untouched** — v3 exe remains the reference build.

## Gate

```
python -m ruff check .
All checks passed!

python tools/lint_tokens.py
token lint: clean (3 stylesheets)

python tests/test_v4_tray.py
ALL PASS (32 checks)

python tests/test_design_v3.py
ALL PASS (48 checks)

pyinstaller nebula-v4.spec
Build complete! -> dist/Nebula-v4.exe
```

## Manual verification

**Build:** `dist/Nebula-v4.exe` — **35.5 MB** (37,202,976 bytes)

**Run:** `dist/Nebula-v4.exe --show` — window renders with full v4 CSS (backdrop, rail, hero card, stat tiles, activity log). Screenshot: `shots/t009-packaged.png`.

**Paths:**
- `dist/logs/obsauto.log` written on launch (1878 bytes this run)
- Repo `config.json` mtime unchanged (`2026-08-03 12:13:22`, 1531 bytes) — exe uses existing `dist/config.json`, does not touch repo root data

**First-build trap (fixed):** initial build showed pywebview 404 for `index.html` because the entry script's `__file__` is `_MEIPASS/app.py`, not `_MEIPASS/spike/app.py`. Bundled assets at `spike/web/` required `RESOURCE_DIR` resolution.

## Notes for review

- Rebuild after any change under `spike/web/` — assets are copied at pack time, not read from source at runtime.
- Frozen exe and `python spike/app.py` share the single-instance mutex; use `--dev` on source runs while testing the exe.
