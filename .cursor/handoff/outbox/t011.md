# t011
status: done

## Changed

- `spike/web/app.css` — `.toggle` sets `flex-direction: row` so boolean switches sit beside their label instead of stacking under it; `.pill` default padding symmetric (`0 16px`) with `.pill:has(.trail)` restoring asymmetric padding for Refresh only
- `spike/web/toast.css` — toast shell/core moved from panel layer (`--panel-*`) to tray layer (`--tray-shell-r`, `--tray-pad`, `--tray-core-r`)

## Gate

```
python tools/lint_tokens.py
token lint: clean (3 stylesheets)

python -m ruff check .
All checks passed!

python tests/test_design_v3.py
ALL PASS (48 checks)
```

## Screenshots

```
python tools/smoke.py --only settings
python tools/smoke.py --only dashboard
```

A live Nebula instance was already running, so the first smoke run photographed that window (pane switch via `goto_pane.txt` only works against the dev instance smoke launches). Re-captured settings with a `--dev` boot and `settings` written to `goto_pane.txt` before launch:

`shots/smoke/settings-t011.png` — Settings / OBS connection: text fields still column-stacked (label + key, input below, hint under). Confirms `.field` untouched.

`shots/smoke/dashboard.png` — "Retry now" primary pill has even left/right padding; no longer reads clipped. "Rescan Steam" Refresh pill still has its trailing circle flush right (`:has(.trail)` path).

**Toast:** radius changed to tray nesting (28 / 6 / 22). Could not photograph — toast needs a live event and no automated hook exists in smoke. CSS uses existing `--tray-*` tokens only.

**Toggles:** fix is the one-line `flex-direction: row` on `.toggle` per the brief's diagnosis. Instant replay / Storage groups hold the booleans; switch beside label on the same row is what the CSS now enforces. OBS tab screenshot confirms non-boolean fields unchanged.
