# t001
status: done

## Changed
- `spike/app.py` — thumbnails as base64 data URLs (WebView2 blocks cross-path `file://`); thumb cache keyed by clip path
- `spike/web/app.css` — merged `.clips-main .card-core.clips-layout` so grid beats the flex rule; by-game column now sits left of the table

## Deliberate deviations
- Play/scissors row action still omitted — reveal + delete only; no new player dependency (per spec note).

## Gate
```
python -m ruff check spike/app.py
All checks passed!

python tools/lint_tokens.py
token lint: clean (1 stylesheet)

python tests/test_v4_tray.py
ALL PASS (31 checks)

python tests/test_design_v3.py
ALL PASS (48 checks)

python tools/shoot.py --out shots/rev_t001_clips.png
Nebula  1898x1156  via PrintWindow  -> shots/rev_t001_clips.png
```

## Notes for review
Two fixes from `shots/rev_t001_clips.png` review:
1. **By-game column** — `.clips-main .card-core { display: flex }` outranked `.clips-layout { display: grid }`, stacking sidebar above the table instead of beside it.
2. **Thumbnails** — `file:///` URIs for `.nebula/thumbs/*.webp` load as broken images in WebView2; inlined as `data:image/webp;base64,…` instead.

Re-verify: `shots/rev_t001_clips.png` — sidebar with game counts + reveal button at bottom; row thumbs render (initials chip when frames absent).
