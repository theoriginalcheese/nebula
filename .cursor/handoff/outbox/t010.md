# t010
status: done

## Changed

- `spike/web/app.js` — `PALETTE_GLYPHS` + `paletteRowGlyph()` / `paletteRowIconHtml()` map icons from `row.action[kind, arg]` only; `paintPalette()` inserts the icon column left of `.palette-label`
- `spike/web/app.css` — `.palette-row-icon` fixed 16px column (empty spacer when no glyph) so label alignment stays stable; `.palette-glyph` gets `line-height: 1` for optical centreing

## Icon mapping (by action kind)

| kind | arg | glyph | codepoint |
|---|---|---|---|
| `goto` | `dashboard` | broadcast / signal | U+E704 |
| `goto` | `clips` | film strip | U+E714 |
| `goto` | `games` | game controller | U+E7FC |
| `goto` | `macropad` | keyboard | U+E765 |
| `goto` | `settings` | sliders | U+E9E9 |
| `transport` | `record` | record ring | U+E7C8 |
| `transport` | `pause` | pause | U+E769 |
| `replay` | `save` / `arm` | Previous (Fluent rewind stand-in) | U+E892 |
| `open` | `recordings` / `config` | folder | U+E8DA |
| `game` | *(any)* | game controller | U+E7FC |

**Deliberately no icon:** any unknown `kind` (empty 16px spacer only). There are no `clip` or settings-field action kinds in the current Api rows — when those land in `palette.py` / `spike/app.py`, this renderer will need a new kind→glyph entry; until then nothing is fabricated.

## Gate

```
python tools/lint_tokens.py
token lint: clean (3 stylesheets)

python -m ruff check .
All checks passed!

python tests/test_palette.py
ALL PASS (43 checks)
```

## Screenshot

```
python spike/app.py --show --url=palette=1
python tools/shoot.py --out shots/t010-palette.png
```

`shots/t010-palette.png` — empty-query palette with icons on every visible row: per-pane glyphs on “Go to …”, record ring on “Record anyway”, Previous on “Arm the buffer”, game controller on Games rows. No blank boxes.

Compared to `design/ui-v3/frames/7e.png`: row icons match the mockup’s intent (record / rewind / folder / controller / pane nav). Fluent has no exact Phosphor `ph-rewind` twin — U+E892 (Previous) is the verified stand-in.
