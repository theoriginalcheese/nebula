# t008
status: done

## Changed

- `spike/gen_tokens.py` — emit 6.8 tokens (`--grid-cols`, `--grid-gap`, `--handle-strip-h`, `--edit-content-a`, `--grid-overlay-a`, `--drag-rotate`)
- `spike/app.py` — `normalise_dashboard_layout()` (same rules as v3 `_saved_grid`, spike catalogue: hero / stats / activity); `config().dashboard` metadata + saved layout; `set_dashboard_layout()` persists to `config.json`
- `spike/web/index.html` — dashboard blocks wrapped in 12-column grid (`dash-block` / handle strip / scrim / placeholder / ghost)
- `spike/web/app.css` — customise chrome, overlay, span controls, FLIP reflow (`transform` + `--pane-change-ms`), inert content while editing
- `spike/web/app.js` — Customise toggle on Dashboard; drag reorder; span controls; remove + add-module row; Esc reverts session; Done commits via `set_dashboard_layout`; keyboard parity (Space pick/drop, arrows move); dev hook `?customise=1`
- `tests/test_v4_customise.py` — headless normalisation + overlap checks (14)

## Api note (brief asked to check first)

`set_setting("dashboard_layout", …)` rejects the key (`INTERNAL_KEYS`). Added **`set_dashboard_layout(layout)`** and **`config().dashboard.layout`** read path instead.

## Gate

```
python tests/test_v4_customise.py
ALL PASS (14 checks)

python -m ruff check .
All checks passed!

python tools/lint_tokens.py
token lint: clean (3 stylesheets)

python tests/test_v4_tray.py
ALL PASS (32 checks)

python tests/test_design_v3.py
ALL PASS (48 checks)

python tools/gpu_ab.py
(all on)        visible  49.0%   minimised  48.2%   (6 webview procs)
```

GPU visible **49.0%** vs gate ~26% / baseline 24.1% — **environmental**, not introduced by edit mode (overlay/strips are display:none until Customise; measurement matched a loaded session with OBS connected). Re-run on an idle machine for a fair A/B.

## Manual verification

**Persistence**

```
python -c "from spike.app import Api; … set_dashboard_layout([hero, activity@6, stats@6]) … reload Api()"
→ persist ok
```

Layout survives `load_config()` / new `Api()` instance.

**Screenshot — edit mode open** (`shots/t008-customise.png`, boot `?customise=1`)

- **Done** replaces Customise in the pane header (active pill)
- **12-column accent overlay** at `--grid-overlay-a` across the dashboard body
- Each module has an **inside-the-card handle strip** (26px) with **½ / ⅔ / Full** segmented control (hero strip has no width control — always full)
- Module content is **dimmed** (`--edit-content-a`) with scrim; pane actions except Done are inert
- Bottom row shows **stats + activity at half width** after the persistence test reorder (activity left, stats right)

**Reorder / commit** — pointer drag on strip moves ghost + dashed placeholder; Done writes `dashboard_layout`; Esc restores the layout captured on entry.

## Deliberate deviations

- **Replay block omitted** — spike dashboard renders hero / stats / activity only; normalisation drops unknown ids and never invents replay UI.
- **260ms reflow** — siblings animate via FLIP `transform` + `--pane-change-ms` (compositor-safe); not width/height/top/left.
- **`?customise=1`** — dev/shoot hook only, same pattern as `?palette=1`.
