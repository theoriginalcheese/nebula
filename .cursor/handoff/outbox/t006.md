# t006
status: done

## Changed

- `spike/web/toast.css`
  - **Panel two-layer card** (18 / 4 / 14) — switched from hero 22/17 and tile family; matches Clips cards in `app.css`
  - **Shell wash + hairline border** retained on `.toast-shell`; core is fill-only on `.toast-core`
  - **Removed `backdrop-filter: blur(18px)`** — hand-typed px, not in contract; two-layer contrast comes from shell/core tokens
  - **Drain bar fades at both ends** — `linear-gradient` on `.toast-drain` over `var(--rule-fade)`; shrink still on `scaleX()`

No markup change — existing `.toast-shell` / `.toast-core` structure was sufficient.

## Radius choice

**Panel** (18 / 14), not hero. Spec default for a 482×150 floating surface; hero 22/17 was the prior pass and read slightly oversized against frame 2i.

## Gate

```
python tests/test_v4_windows.py
ALL PASS (16 checks)

python -m ruff check .
All checks passed!

python tools/lint_tokens.py
token lint: clean (3 stylesheets)

python tests/test_v4_tray.py
ALL PASS (31 checks)

python tests/test_design_v3.py
ALL PASS (48 checks)
```

## Screenshot vs frame 2i

**Reference:** `design/ui-v3/frames/2i.png`

**Capture:** `shots/t006-toast.png` — start event via `python -m spike.windows`, ember ring icon, "Recording started" / "Helldivers 2", drain ~20% through life.

| Check | Result |
|-------|--------|
| Drain bar ends fade | **Yes** — ember line soft-feathers at left and right over ~32px; no hard horizontal caps at track width |
| Two layers distinguishable | **Yes** — lighter hairline-washed shell ring visible around the darker core; matches Clips card nesting |
| Panel radius (not tile) | **Yes** — 18/14 panel family; corners read rounded without the prior blocky tile feel |
| Icon + type hierarchy | Unchanged — ember chip, title/sub scale intact |
| Behaviour | Unchanged — replace-in-place, single tick chain, 4s drain |

## Deliberate deviations

none
