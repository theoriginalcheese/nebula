# Web build (Add to Home Screen): the app server proxies /v1 same-origin.
EXPO_PUBLIC_AGENT_PROXY=1

# Native build uses eas.json env instead; for a local native test, set:
# EXPO_PUBLIC_AGENT_URL=http://alien-pc.tail25e601.ts.net:8766
